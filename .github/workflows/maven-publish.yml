name: ShieldNet CI/CD - Maven Package

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: 'maven'
        server-id: github
        settings-path: ${{ github.workspace }}

    - name: Build ShieldNet Services
      run: |
        for service in shieldnet-posts shieldnet-auth; do
          echo "Building $service..."
          mvn -B clean package --file $service/pom.xml
        done

    - name: Publish to GitHub Packages
      if: github.event_name == 'release'
      run: |
        for service in shieldnet-posts shieldnet-auth; do
          echo "Deploying $service..."
          mvn deploy -s $GITHUB_WORKSPACE/settings.xml --file $service/pom.xml
        done
      env:
        GITHUB_TOKEN: ${{ github.token }}
